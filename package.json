{
  "name": "rex-server",
  "version": "1.2.1",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc --build && tsc-alias && touch dist/master/master.pid  && cp -r ./src/logs ./src/root dist",
    "stop": "ps aux | grep node | awk '{print $2}' | xargs kill",
    "prepublishOnly": "npm run build",
    "start":"node dist/cli/main.js stop && npm run build && node dist/cli/main.js start"
  },
  "bin": {
    "rex": "dist/cli/main.js"
  },
  "keywords": [],
  "author": "Raghvendra Misra",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.6",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@types/mime-types": "^2.1.4",
    "chalk": "^4.1.2",
    "commander": "^13.0.0",
    "is-prime-number": "^2.1.0",
    "mime-types": "^2.1.35",
    "winston": "^3.17.0",
    "yaml": "^2.7.0",
    "zod": "^3.24.1"
  }
}
