{
  "name": "rex-server",
  "version": "1.3.2",
  "description": "Rex Server is a Node.js-based reverse proxy server available as an npm package. It allows you to handle HTTP and HTTPS traffic, route requests to upstream servers, and manage worker processes efficiently. With its CLI interface, Rex makes it easy to configure and run a proxy server with custom settings.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc --build && tsc-alias && touch dist/conf/master.pid  && cp -r ./src/logs ./src/static dist && cp ./src/conf/mock.rex.config.yaml ./src/conf/rex.config.yaml dist/conf/",
    "stop": "ps aux | grep node | awk '{print $2}' | xargs kill",
    "prepublishOnly": "ts-node src/scripts/updateCliVersion && npm run build",
    "start":"  node dist/cli/main.js stop && npm run build && node dist/cli/main.js start"
  },
  "bin": {
    "rex": "dist/cli/main.js"
  },
  "keywords": [
    "server",
    "rex",
    "reverse-proxy",
    "load-balancer",
    "node"
  ],
  "repository": {"url": "https://github.com/dev-raghvendramisra/Rex-Server"},
  "author": "Raghvendra Misra",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.6",
    "@types/mime-types": "^2.1.4",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^13.0.0",
    "mime-types": "^2.1.35",
    "winston": "^3.17.0",
    "yaml": "^2.7.0",
    "zod": "^3.24.1"
  }
}
